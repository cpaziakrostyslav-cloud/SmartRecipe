<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:SmartRecipe.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="using:SmartRecipe.Views"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="SmartRecipe.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Icon="/Assets/avalonia-logo.ico"
        Title="SmartRecipe">

    <Grid ColumnDefinitions="250, *">
        
        <Border Grid.Column="0" Background="#2C3E50" Padding="15">
            <StackPanel Spacing="10">
                <TextBlock Text="SmartRecipe" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Foreground="White" 
                           Margin="0,20,0,40"
                           HorizontalAlignment="Center"/>

                <Button Content="Мій холодильник" 
                        Command="{Binding NavigateToFridgeCommand}"
                        HorizontalAlignment="Stretch" 
                        HorizontalContentAlignment="Left"
                        Background="Transparent" 
                        Foreground="White" 
                        FontSize="16" 
                        Padding="15,10"/>
                
                <Button Content="Каталог рецептів" 
                        Command="{Binding NavigateToRecipesCommand}"
                        HorizontalAlignment="Stretch" 
                        HorizontalContentAlignment="Left"
                        Background="Transparent" 
                        Foreground="White" 
                        FontSize="16" 
                        Padding="15,10"/>
                
                <Separator Background="#34495E" Margin="0,10" Height="2"/>
                
                <Button Content="Налаштування" 
                        Command="{Binding NavigateToSettingsCommand}"
                        HorizontalAlignment="Stretch" 
                        HorizontalContentAlignment="Left"
                        Background="Transparent" 
                        Foreground="#BDC3C7" 
                        FontSize="16" 
                        Padding="15,10"/>
            </StackPanel>
        </Border>
        
        <Border Grid.Column="1" Background="#F5F6FA">
            <TransitioningContentControl Content="{Binding CurrentPage}" 
                                         Padding="20"/>
        </Border>   
    </Grid>

</Window>
